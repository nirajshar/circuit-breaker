{
  "name": "Test_App",
  "version": "1.0.0",
  "description": "Microservice App which can fail when loaded due to Internal failure OR System unavailability",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "tsc",
    "start-server": "npm run build && node build/index.js",
    "test-breaker": "npm run build && node build/test.js"
  },
  "keywords": [],
  "author": "Niraj Sharma",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/express": "^4.17.11",
    "typescript": "^4.2.4"
  }
}
